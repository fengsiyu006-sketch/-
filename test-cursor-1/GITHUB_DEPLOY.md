# 🚀 GitHub Pages 部署指南

## 📁 需要复制的文件夹

**复制整个 `test-cursor` 文件夹的内容到GitHub仓库根目录**

### 具体文件列表：

```
你的GitHub仓库根目录/
├── index.html          ✅ 必需
├── styles.css          ✅ 必需
├── script.js           ✅ 必需
├── images imgs/       ✅ 必需（图片文件夹）
│   ├── 3cece2e959e04ca60dea36782e3c428a.jpg
│   └── 3e7014efc9e1936f5070576b4f759cae.jpg
└── weixin.jpg         ✅ 可选（微信二维码）
```

## 📝 部署步骤

### 1. 复制文件到GitHub仓库

**方法A：使用Git命令**
```bash
# 进入test-cursor文件夹
cd test-cursor

# 复制所有文件到GitHub仓库
# （假设你的GitHub仓库在 ../your-repo-name）
cp -r * ../your-repo-name/
cp -r "images imgs" ../your-repo-name/
```

**方法B：手动复制**
- 打开 `test-cursor` 文件夹
- 全选所有文件和文件夹
- 复制到你的GitHub仓库根目录

### 2. 调整路径（重要！）

由于代码中使用的是 `../images imgs/`，如果部署到根目录，需要改为 `images imgs/`

**需要修改的文件：**
- `script.js` - 第22行和第26行
- `index.html` - 第120行

**修改方法：**
将 `../images imgs/` 改为 `images imgs/`

### 3. 提交到GitHub

```bash
cd your-repo-name
git add .
git commit -m "部署个人网站"
git push origin main
```

### 4. 启用GitHub Pages

1. 进入你的GitHub仓库
2. 点击 **Settings**（设置）
3. 左侧菜单找到 **Pages**
4. **Source** 选择 `main` 分支
5. **Folder** 选择 `/ (root)`
6. 点击 **Save**

### 5. 访问网站

等待1-2分钟后，访问：
```
https://你的用户名.github.io/仓库名/
```

## ⚠️ 重要提示

1. **文件夹名称**：`images imgs` 中间有空格，复制时注意保持名称一致
2. **路径问题**：如果图片不显示，检查路径是否正确
3. **文件大小**：确保单个文件不超过100MB
4. **等待时间**：GitHub Pages 部署可能需要几分钟

## 🔧 如果遇到问题

### 图片不显示？
- 检查 `images imgs` 文件夹是否已复制
- 检查路径是否正确（应该是 `images imgs/` 而不是 `../images imgs/`）
- 检查文件名是否完全匹配

### 网站无法访问？
- 确认 GitHub Pages 已启用
- 检查仓库是否为公开仓库（免费版需要公开）
- 等待几分钟让GitHub完成部署

## 📦 快速部署清单

- [ ] 复制 `test-cursor` 文件夹所有内容到GitHub仓库根目录
- [ ] 调整图片路径（`../images imgs/` → `images imgs/`）
- [ ] 提交代码到GitHub
- [ ] 启用GitHub Pages
- [ ] 等待部署完成
- [ ] 测试网站功能
